var React = require('react');


var Book = React.createClass({

  render: function(){

    return(
      <section className="banana" id="banana">
        <h1>This is the book show page</h1>
      </section>

    )
  }
})
module.exports = Book;
